[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "cic_tokenizer"
version = "0.1.0"
description = "Universal CIC Tokenizer powered by Exact Information Geometry"
requires-python = ">=3.8"
